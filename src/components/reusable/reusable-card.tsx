import React, { useState } from "react";
import styled from "styled-components";

const ReusableCard = () => {
  return <ReusableCardWrapper></ReusableCardWrapper>;
};

export default ReusableCard;

const ReusableCardWrapper = styled.div`
  width: 100%;
  height: 100%;
  border: 0;
`;
